{
  "name": "mimic-chat",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy dist --project-name mimiclog",
    "deploy+": "node scripts/deploy-plus.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "^3.1.0",
    "dice-roller-parser": "^0.1.8",
    "dompurify": "^3.4.2",
    "marked": "^18.0.3"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.4",
    "svelte": "^5.55.2",
    "vite": "^5.0.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.78.0"
  }
}
